services:
  quant-trading:
    build: .
    container_name: quantitative-trading
    env_file:
      - .env
    environment:
      - TZ=Asia/Taipei
    volumes:
      # 行情缓存与回测输出持久化到宿主机
      - ./data:/app/data
      - ./output:/app/output
    command: ["python", "run_demo.py", "--offline"]
